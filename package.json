{
  "name": "against-the-grains-root",
  "version": "0.1.0",
  "description": "A full-stack e-commerce application for selling grain-free food and merchandise.",
  "private": true,
  "main": "server.mjs",
  "engines": {
    "node": "20.15.0",
    "npm": "10.8.1"
  },
  "scripts": {
    "dev": "vite",
    "start": "node server.mjs",
    "develop": "concurrently \"cd server && npm run devStart\" \"cd client && npm run dev\"",
    "build": "cd client && npm run build && cd ..",
    "prestart": "npm run build",
    "postinstall": "cd server && npm install && cd ../client && npm install"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-session": "^1.18.0",
    "mysql2": "^3.10.3",
    "sequelize": "^6.37.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "vite": "^5.3.4"
  }
}
